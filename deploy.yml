name: Deploy Website

on:
  push:
    branches:
      - main  # أو قم بتحديد الفرع الذي تريد متابعة التغييرات عليه

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'  # اختر نسخة Node.js المناسبة لمشروعك

    - name: Install dependencies
      run: npm install  # أو أي أمر مناسب لمشروعك

    - name: Deploy to Server
      run: |
        # أضف هنا أوامر التوزيع الخاصة بموقعك (مثل نقل الملفات إلى الخادم)
        echo "Deploying site..."
